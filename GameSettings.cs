//This is used to set our game resolution. Its constant so we can get the width and height from wherever.
static class GameSettings
{
    public const int GAME_WIDTH = 90;
    public const int GAME_HEIGHT = 30;    
}
